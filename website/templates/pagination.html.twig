<div class="d-flex justify-content-center">
    <ul class="pagination">
        <li class="page-item {% if page == 1 %}disabled{% endif %}">
            <a class="page-link" href="{{path(route, {'page': page - 1, 'league_name': league})}}">&laquo;</a>
        </li>
        {% for p in 1..nbPages %}
            <li class="page-item {% if page == p %}active{% endif %}">
                <a class="page-link" href="{{ path(route, {'page': p, 'league_name': league}) }}">{{ p }}</a>
            </li>
        {% endfor %}
        <li class="page-item {% if page == nbPages %}disabled{% endif %}">
            <a class="page-link" href="{{path(route, {'page': page + 1, 'league_name': league})}}">&raquo;</a>
        </li>
    </ul>
</div>