{% extends 'admin/base.html.twig' %}

{% block title %}Administration des produits{% endblock %}

{% block body %}
    <link rel="stylesheet" href="{{ asset('build/form.css') }}">

    <div class="container">
        <h2 class="deepshadow">Gestion des produits</h2>
        <div class="login-form bg-light py-3 px-3">
            <table class="table table-hover">
                <thead>
                <tr>
                    <th>ID</th>
                    <th>Équipe</th>
                    <th>Statut</th>
                    <th>Prix</th>
                    <th>Stock</th>
                    <th>Action</th>
                </tr>
                </thead>
                <tbody>
                {% for product in products %}
                    <tr>
                        <td>{{ product.id }}</td>
                        <td><img src="{{ product.productImg }}" class="avatar" alt="img">{{ product.team.teamName }}</td>
                        <td>{{ product.state.state }}</td>
                        <td>{{ product.price }}</td>
                        <td>{{ product.stock }}</td>
                        <td>
                            <a href="#" class="btn btn-primary"><i class=" fas fa-pen"></i></a>
                            <a href="#" class="btn btn-danger"><i class=" fas fa-trash"></i></a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
