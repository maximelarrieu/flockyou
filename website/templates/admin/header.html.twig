<!--Navbar-->
<nav class="navbar navbar-expand-lg navbar-dark bg-danger">

    <!-- Navbar brand -->
    <a class="navbar-brand" href="{{ path('home') }}"><img src="{{asset('build/images/ressources/flockyou.png')}}" title="flockyou" alt="logo" style="width: 200px;" class="mr-5"></a>

    <!-- Collapse button -->
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#basicExampleNav"
            aria-controls="basicExampleNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible content -->
    <div class="collapse navbar-collapse" id="basicExampleNav">

        <!-- Links -->
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_dashboard') }}"><i class="fas fa-chart-line mr-1"></i>Dashboard</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_teams') }}"><i class="fas fa-futbol mr-1"></i>Equipes</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_products', {'page': 1}) }}"><i class="fas fa-tshirt mr-1"></i>Produits</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_users', {'page': 1}) }}"><i class="fas fa-users mr-1"></i>Utilisateurs</a>
            </li>
        </ul>

        <!-- Links -->
        <i class="fas fa-search m-3" style="color: white"></i>
        <form class="form-inline">
            <div class="md-form my-0 mr-5">
                <input class="form-control mr-sm-2" type="text" placeholder="Rechercher..." aria-label="Search">
            </div>
        </form>
        {% if app.user %}
            <a href="{{ path('account', {'username' : app.user.username}) }}"><i class="fas fa-user m-3" style="color: white"></i></a>
            <li class="nav-item">
                <a href="{{ path('admin_logout') }}" class="nav-link"><i class="fas fa-sign-out-alt" style="color: white"></i></a>
            </li>
        {% endif %}
    </div>
    <!-- Collapsible content -->

</nav>
<!--/.Navbar-->