{% extends 'admin/base.html.twig' %}

{% block title %}Modifier le flocage {{ flocage.flocage }}{% endblock %}

{% block body %}
    <link rel="stylesheet" href="{{ asset('build/form.css') }}">

    <div class="container">
        <h2 class="deepshadow">Modifier le flocage</h2>
        <div class="login-form bg-light py-3 px-3">

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-success">Modifier le flocage</button>
            {{ form_end(form) }}

        </div>
    </div>

{% endblock %}