{% extends 'admin/base.html.twig' %}

{% block title %}Modifier l'utilisateur{{ user.username }}{% endblock %}

{% block body %}
    <link rel="stylesheet" href="{{ asset('build/form.css') }}">

    <div class="container">
        <h2 class="deepshadow">Modifier l'utilisateur</h2>
        <div class="login-form bg-light py-3 px-3">

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-success">Modifier l'utilisateur</button>
            {{ form_end(form) }}

        </div>
    </div>

{% endblock %}