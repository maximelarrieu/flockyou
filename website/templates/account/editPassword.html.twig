{% extends "base.html.twig" %}

{% block title %}Modification du mot de passe{% endblock %}

{% block body %}
    <link rel="stylesheet" href="{{ asset("build/form.css") }}"/>

    <div class="container">
        <h2 class="deepshadow">Modification du mot de passe</h2>
        <div class="login-form bg-light py-3 px-3">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-success">Modifier le mot de passe</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}